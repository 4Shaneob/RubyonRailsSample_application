$ git status
$ git add -A
$ git commit -m "comment description"
$ git push -u origin static-pages #to static pages branch
$ git push #to main branch
.
#to update a branch after changes to the Master
$ git fetch
$ git rebase origin/master